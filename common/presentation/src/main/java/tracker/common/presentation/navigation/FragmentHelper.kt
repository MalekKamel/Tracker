package tracker.common.presentation.navigation

import com.sha.kamel.navigator.modular.AddressableFragment

object Fragments {

    object MapFrag: AddressableFragment {
        override val className: String = "tracker.feature.map.MapFrag"
    }

}